<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Blog Title</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            position: relative; /* 让 ::before 伪元素相对于 body 定位 */
        }

        body::before {
            content: "";
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1; /* 使伪元素在内容之后，避免遮挡 */
            background-image: url('<%- theme.blogBackgroundImage %>');
            background-size: <%- theme.blogBackgroundSize %>;
            filter: blur(5px); /* 添加简单的模糊效果 */
            /* 这里可以添加其他样式属性，例如透明度 background-color: rgba(255, 255, 255, 0.5); */
        }

        /* 其他样式... */
    </style>
</head>

<body>
    <div id="index">
        <% page.posts.each(function(post){ %>
            <!-- 博客文章列表项 -->
            <div class="index-li">
                <!-- 链接到文章详情页 -->
                <a href="<%- url_for(post.path) %>">
                    <!-- 文章标题 -->
                    <h2 class="index-title">
                        <%- post.title || post.slug.split("/")[post.slug.split("/").length-1] %>
                    </h2>
                    <!-- 文章摘要 -->
                    <div class="index-brief">
                        <%- post.more.replace(/<\/?.+?\/?>/g, "").substring(0, 150) %>
                            ...
                    </div>
                </a>
                <!-- 文章元信息 -->
                <div class="article-meta">
                    <span><%- date(post.date, "YYYY-MM-DD" ) %></span>
                    <% if (post.categories) { %>
                        <% for (var j=0; j < post.categories.data.length; j++) { %>
                            <% if (post.categories.data[j] && post.categories.data[j].name) { %>
                                <!-- 文章分类 -->
                                <span>
                                    <i class="fa fa-folder" aria-hidden="true"></i>
                                    <a href="/<%- post.categories.data[j].path %>"><%- post.categories.data[j].name %></a>
                                    <% if (j < post.categories.data.length - 1) { %>
                                        >
                                    <% } %>
                                </span>
                            <% } %>
                        <% } %>
                    <% } %>
                </div>
            </div>
        <% }); %>

        <!-- 文章分页 -->
        <div id="index-paginator">
            <% if (page.total > 1){ %>
                <%- paginator({ prev_text: '<' , next_text: '>' }) %>
            <% } %>
        </div>
    </div>
</body>

</html>